Write a program that takes an array of numbers and replaces a negative array number with the string "floodgate".

For example if the array x is initially after the program
is done that array should be ['floodgate', 'floodgate', 2]

var x: [-1, -3, 2];

function negativeToFloodgate(x) {
for(i=0; i < x.length; i++) {
 if(x[i] < 0){
  x[i] = "floodgate";
}
  }
console.log(x);

}

negativeToFloodgate(x);